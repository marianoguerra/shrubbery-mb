.DS_Store
_build/
target/
target
.mooncakes/
.moonagent/

# The reference checkout: read-only material, never a build or CI input.
# Restore with tools/fetch-reference.sh.
/reference

# Built by `just playground-build`, next to the page that loads it.
/playground/*.wasm

# Local dev-server config for the browser tooling.
/.claude/
